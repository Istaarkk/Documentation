```bash
ROOT=$(realpath squashfs2-root)
  AFL_SKIP_CPUFREQ=1 AFL_I_DONT_CARE_ABOUT_MISSING_CRASHES=1 \
  AFL_QEMU_LD_PREFIX="$ROOT" QEMU_LD_PREFIX="$ROOT" \
  afl-fuzz -Q -i seeds -o out -- "$ROOT/bin/ash" -n
```


```bash
unset AFL_QEMU_LD_PREFIX 
  QEMU_LD_PREFIX="$ROOT" AFL_PATH="$AFL_PATH" \
  AFL_SKIP_CPUFREQ=1 AFL_I_DONT_CARE_ABOUT_MISSING_CRASHES=1 \
  afl-fuzz -Q -i in -o out -- "$ROOT/bin/ash" -n
```